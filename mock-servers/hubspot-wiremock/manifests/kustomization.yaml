apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: hubspot-wiremock-mappings
    files:
      - mappings/hubspot-contacts-success.json
      - mappings/hubspot-create-contact-success.json
