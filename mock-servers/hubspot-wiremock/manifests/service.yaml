apiVersion: v1
kind: Service
metadata:
  name: api-hubapi-com
  labels:
    app.kubernetes.io/name: hubspot-wiremock
    app.kubernetes.io/part-of: outbound-api-sandbox
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hubspot-wiremock
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
