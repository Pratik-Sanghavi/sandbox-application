apiVersion: v1
kind: ConfigMap
metadata:
  name: sandbox-mock-routing
  labels:
    app.kubernetes.io/name: sandbox-mock-routing
    app.kubernetes.io/part-of: outbound-api-sandbox
data:
  mocked-hosts: |
    # One external hostname per line. The addon derives its internal Service
    # name by replacing dots with hyphens in mock-servers-ns-1.
    api.hubapi.com